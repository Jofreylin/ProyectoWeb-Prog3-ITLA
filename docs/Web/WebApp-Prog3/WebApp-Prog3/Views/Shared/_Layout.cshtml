
@using WebApp_Prog3.Models;

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700|Roboto+Slab:400,700|Material+Icons" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/latest/css/font-awesome.min.css">

</head>
<body class="">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-lg sticky-top">

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto ">
                <li class="nav-item">
                    @Html.ActionLink("INICIO", "Index", "Home", null, new { @class = "nav-link" })
                </li>

                @*<li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Categorias
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                @{
                    CategoriaClient categoria = new CategoriaClient();
                    var elementos = categoria.GetAll();
                    int count = 0;
                    foreach (var cat in elementos)
                    {
                        @Html.ActionLink(cat.Nombre, "Busqueda", new { buscar = cat.Nombre, sortOrder = ViewBag.SortOrder == null ? "Asc" : (ViewBag.SortOrder == "Asc" ? "Desc" : "Asc"), sortBy = "Fecha" }, new { @class = "dropdown-item" })
                        count++;
                        if (count == 4)
                        {
                            break;
                        }

                    }
                    <div class="dropdown-divider"></div>
                    @Html.ActionLink("Ver todas", "Categorias", "Home", null, new { @class = "dropdown-item" })
                }

            </div>
        </li>*@

                <li class="nav-item">
                    @Html.ActionLink("Categorias", "Categorias", "Home", null, new { @class = "nav-link" })
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"> </a>
                </li>
                <li class="nav-item container">
                    @using (Html.BeginForm("Busqueda", "Home", FormMethod.Post, new { @class = "form-inline" }))
                    {

                        @Html.TextBox("buscar", null, new { @class = "form-control mr-sm-4", @placeholder = "Algo mas especifico" })
                        <button class="btn btn-outline-primary " type="submit">Buscar</button>

                    }
                </li>

            </ul>
            <form class="form-inline my-2 my-lg-0 ">
                <button class="btn btn-success mr-sm-5 float-right">Postear un trabajo</button>
                @if (User.Identity.IsAuthenticated)
                {
                    if (User.IsInRole("Admin"))
                    {
            <a>@Html.ActionLink(" Perfil", "ProfileAcc", "UserAdmin", null, new { @class = "fa fa-user btn btn-warning my-2 my-sm-0" })</a>
                    }
                    else if (User.IsInRole("Poster"))
                    {
                        <a>@Html.ActionLink(" Perfil", "ProfileAcc", "UserPoster", null, new { @class = "fa fa-user btn btn-warning my-2 my-sm-0" })</a>
                    }
                }
                else
                {
                    <a>@Html.ActionLink("Iniciar Sesion", "Index", "UserPoster", null, new { @class = "btn btn-outline-secondary mr-sm-3" })</a>
                    <a>@Html.ActionLink("Registrarse", "Index", "UserPoster", null, new { @class = "btn btn-outline-info my-2 my-sm-0 btn-sm" })</a>
                }

            </form>
            @*<ul class="navbar-nav mr-sm-4 ">
            <li>
                <a href="#" class="btn btn-info btn-lg active" role="button" aria-pressed="true">Register</a>
            </li>
            <li>
                <a href="#" class="btn btn-secondary btn-lg active" role="button" aria-pressed="true">Login</a>
            </li>
        </ul>*@
        </div>
    </nav>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - TEAM 4 PROGRAMACION 3</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
